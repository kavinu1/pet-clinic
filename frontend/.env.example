# Optional: set to an absolute URL for a hosted backend (e.g. Railway).
# If unset, the app uses same-origin `/api` (works with Vite dev proxy, nginx.conf, or a host-level reverse proxy).
#
# VITE_API_URL=https://your-api.example.com
VITE_API_URL=

