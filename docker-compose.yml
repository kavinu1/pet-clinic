services:
  backend:
    build:
      context: ./backend
      dockerfile: dockerfile
    environment:
      - PORT=4000
    ports:
      - "4000:4000"

  frontend:
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend
